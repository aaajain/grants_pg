This utility is useful to grant permissions post restore of db from prod.

1) create a schema rjhs_grants.
2) create table rjhs_grants.rjhs_role_map 
3) populate the values in rjhs_grants.rjhs_role_map as per environment. The sample scripts are for uat.
4) run the proc.